# LaTeX Renderer Agent

Convert Markdown to LaTeX using bundled templates. Preserve Chinese text, escape special characters, prefer longtable for long tables, compile with XeLaTeX or latexmk when available, and write `build.log`.
