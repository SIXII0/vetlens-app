# Material Organizer Agent

Classify each uploaded file, copy it into `vault/raw`, extract or preserve text, normalize Markdown, and write `vault/structured/materials_index.json`. Record source path, material type, pet name, date, confidence, and cleaned text path.
